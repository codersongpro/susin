# 신통픽 작업 규칙

수신픽 + 소통픽 = 신통픽. 충북 교사가 수신자를 고르는 두 가지 일을 한 앱에서 한다.

| 도구 | 어디에 | 하는 일 |
|---|---|---|
| **소통픽** | 소통메신저 | [사용자 선택] 창에서 수신자를 자동으로 골라 담는다 |
| **수신픽** | 에듀파인 | 개인수신그룹 일괄등록 엑셀을 만든다 |

'출구', '모드' 같은 말 대신 **소통픽 / 수신픽**이라고 부른다. 앱·랜딩페이지·문서 모두.

---

## 빌드·릴리즈는 허락받고 한다 (중요)

**사용자가 명시적으로 말하기 전에는 빌드도 릴리즈도 돌리지 않는다.**
Actions 워크플로를 임의로 실행하지 않는다. 버전을 올리지도 않는다.

코드를 고친 뒤에는 여기까지만 한다.

1. 테스트를 돌려 통과를 확인한다
2. 커밋하고 작업 브랜치와 `main` 둘 다 push
3. 무엇을 고쳤는지 알리고, 릴리즈할지 **물어본다**

"이제 릴리즈해줘", "빌드해줘" 같은 말이 있을 때만 아래 절차를 밟는다.

## 릴리즈 절차

**릴리즈할 때는 반드시 `CHANGELOG.md` 를 먼저 갱신한다.** 여기 적은 내용이 그대로
GitHub Release 본문이 된다. 빠뜨리면 릴리즈 노트에 "CHANGELOG.md 에 없습니다" 가 찍힌다.

순서:

1. `CHANGELOG.md` 맨 위에 `## vX.Y.Z` 단락을 쓴다 (사용자가 읽을 말로, 개발 용어 말고)
2. `python3 tools/bump_version.py X.Y.Z` — main.py 와 index.html 버전을 한꺼번에 맞춘다
3. `python3 -m unittest discover -s tests` 로 전부 통과 확인
4. 커밋하고 `claude/...` 작업 브랜치와 `main` **둘 다** push
5. Actions → 릴리즈 빌드 → Run workflow 에서 `version` 에 `vX.Y.Z`

릴리즈 노트 = `CHANGELOG.md` 의 해당 단락 + `docs/release_footer.md` (받는 방법·백신 안내).
고정 안내문을 고칠 일이 있으면 `docs/release_footer.md` 를 고친다. 워크플로에 본문을
박아 두지 않는다.

이 저장소는 태그 push 권한이 없는 세션에서도 릴리즈할 수 있게 `workflow_dispatch` 의
`version` 입력으로 러너가 태그까지 만든다.

## 랜딩페이지도 같이 고친다

앱 화면이나 동작을 바꿨으면 `index.html` 에 그 내용이 반영됐는지 **매번 확인하고 같이
고친다.** 사용자가 따로 말하지 않아도 한다.

특히 아래가 어긋나기 쉽다.

- 탭 이름과 개수, 화면 배치 (사용 방법 절과 앱 미리보기)
- 버전 표기 (`tools/bump_version.py` 가 맞춰 주지만 문구는 사람이 본다)
- FAQ 에 적힌 동작 설명 (없앤 기능이 FAQ 에 남아 있던 적이 있다)
- 기능 카드 설명

## 문체

사용자에게 보이는 모든 글(앱 문구, 랜딩페이지, 릴리즈 노트, README)에 적용한다.

- **제목이나 문장에 대시(—)를 쓰지 않는다.** 괄호나 문장으로 푼다.
  `명단 입력 — 두 가지 방법` (X) → `명단 넣기 (둘 중 편한 쪽으로)` (O)
- 화살표로 조작을 이어붙이지 않는다.
  `[캡처 시작] 클릭 → 마우스 이동 → Enter로 확정` (X)
  → `[캡처 시작] 을 누르고 마우스를 옮긴 뒤 Enter 를 누릅니다` (O)
- 광고 카피체를 쓰지 않는다. `지금 바로 시작하세요`, `반복 작업, 이제 맡기세요` 같은 것.
- `~처리합니다`, `~제공합니다` 같은 번역투 대신 사람이 말하는 쪽으로.
- 영어 나열을 피한다. `Teacher · Data Analytics` (X) → `초등교사 · 데이터 분석` (O)
- **`CHANGELOG.md` 는 명사형으로 짧게 쓴다.** 목록 한 줄에 한 가지씩, 무엇이 달라졌는지만.
  `~했습니다` 로 풀어쓴 문단 (X) → `추가됐는지 확인하고 넘어감` (O)
  앱 화면과 랜딩페이지는 지금처럼 존댓말 문장으로 쓴다. 명사형은 릴리즈 노트에만 쓴다.

## 안전 규칙 (공문이 엉뚱한 곳으로 가는 것을 막는다)

이건 취향이 아니라 이 앱의 존재 이유다. 완화하지 않는다.

- **짐작한 결과를 자동으로 확정하지 않는다.** 정확 일치·약칭·접두어까지만 자동이고,
  편집거리로 찾은 것은 사람이 골라야 엑셀에 들어간다.
- **같은 이름이 여럿이면 아무것도 고르지 않는다.** `행정과` 는 11곳에 있다.
  후보를 보여주고 사람이 고르게 한다.
- **기관 해석 경로에 퍼지 매칭을 넣지 않는다.** 청주와 충주는 한 글자 차이다.
  `sotong_parser.lookup_org` 는 퍼지를 쓰므로, 해석에는 `lookup_org_graded` 를 쓰고
  `AUTO_GRADES` 만 받는다.
- **빠지는 것은 반드시 알린다.** 코드가 없어 엑셀에서 제외되는 기관은 사유와 함께 보여준다.
- `org_codes.json` 의 `기관` 은 전체경로가 키다. 짧은 이름을 키로 쓰면 부서명이 겹쳐
  뒤엣것이 앞엣것을 덮어쓴다.

## 코드

- 좌표(마우스 위치)는 **소통픽에서만** 쓴다. 수신픽에 좌표 기능을 넣지 않는다.
- 결과 엑셀은 `assets/수신그룹_양식.xlsx` 를 열어 값만 채운다. 새로 만들지 않는다.
  에듀파인 서버가 Apache POI 로 읽으므로 원본 구조를 지키는 편이 안전하다.
- `tools/merge_neis.py`, `tools/merge_edufine_orgs.py` 는 기존 별칭을 덮어쓰지 않고
  추가만 한다. 반복 실행해도 안전해야 한다.
- 커밋 메시지는 한국어로, 무엇을 왜 고쳤는지 쓴다.

## 테스트

```bash
python3 -m unittest discover -s tests
```

- tkinter 없이 돈다. `tests/test_app_flow.py` 가 동작하는 스텁으로 App 을 만들어
  탭 구성·명단 추출·엑셀 대상 목록까지 확인한다.
- **스텁을 실제 tkinter 처럼 동작하게 유지한다.** 조용히 넘어가는 스텁 때문에
  탭이 사라지는 버그를 두 번 놓쳤다. 등록되지 않은 탭에는 예외를 던져야 한다.
- GUI 를 실제로 띄우는 확인은 `tools/smoke_gui.py` 가 CI(Windows)에서 한다.
  탭 개수와 이름까지 검사하므로, 화면 구성을 바꾸면 여기도 같이 고친다.
- 설정 파일을 건드리는 테스트는 `app_config.CONFIG_FILE` 을 임시 경로로 돌려놓는다.
  안 그러면 개발자의 실제 설정이 날아간다.

## CI 에서 조심할 것

- Windows 러너 콘솔은 cp1252 다. 한글을 찍는 스크립트는 죽는다.
  워크플로 잡에 `PYTHONUTF8` 이 걸려 있고, 스크립트도 stdout 을 utf-8 로 다시 연다.
- `tools/` 안의 스크립트는 저장소 루트를 `sys.path` 에 넣어야 `main` 을 찾는다.
